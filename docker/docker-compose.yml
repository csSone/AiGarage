services:
  nginx:
    image: nginx:latest
    volumes:
      - ../nginx/nginx.conf:/etc/nginx/nginx.conf
      - ../nginx/html:/usr/share/nginx/html
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    container_name: aigarage-nginx
